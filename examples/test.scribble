import("sys")

func main() {
	//var fibsResult := recurse(125);
	//sys.Write( sys.String ( fibsResult ) );
	//sys.Write(" results\n");
	//heap.Test();
	//bubble.Test();
	
	var arr := [100] int;
	
	for var i := 0; i < 100; i++ do {
	}
}