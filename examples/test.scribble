import("sys")
import("sorts/bubble")

func main() {

//	var arr := [1000]int;
//	Triy(arr);
//	Sort(arr);
	
//	for var i := 0; i < len(arr); i++ do {
//		sys.Write( sys.Concat ( sys.String( arr[i] ), " " ) );
//	}
	
//	sys.Write( "\n" );

//	heap.Test();
	bubble.Test();
}