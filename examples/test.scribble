import("sys")

type A := struct {
	Name : string
	Age : int
}

func j() : int {
	return 3;
}

func r() : int {
	return j() + 4;
}

func y() : int {
	return r() + 123;
}

func fibs(n : int) : int {
	if n = 0 then return 0;
	if n = 1 then return 1;
	return fibs(n-1) + fibs(n-2);
}

func recurse(n : int) : int {
	if n = 0 then return 0;
	return n + recurse(n-1);
}

func main() {

//	sys.Write( sys.Concat( sys.String( fibs(700) ), "\n") );
	var hello := recurse( 200 );
	sys.Write ( sys.String ( recurse(200) ) );
}