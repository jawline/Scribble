import("sys")
import("primes/sieve")

func j() : int {
	return 3;
}

func r() : int {
	return j() + 4;
}

func y() : int {
	return r() + 123;
}

func main() {

	for var i := 0; i < 10; i++; {
		sys.Write( sys.String ( y() ) );
		sys.Write( "\n" );
	}
	
	var j := 0;
	j := j + i;
	
	var primesSieve := sieve.Sieve(100);
	
	for i := 0; i < 100; i++; {
		sys.Write( sys.String(i) );
		sys.Write(" ");
		sys.Write( sys.String ( primesSieve[i] ) );
		sys.Write( "\n" );
	}
	
}