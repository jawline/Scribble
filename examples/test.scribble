import("sys")

type A := struct {
	Name : string
	Age : int
}

func j() : int {
	return 3;
}

func r() : int {
	return j() + 4;
}

func y() : int {
	return r() + 123;
}

func main() {

	for var i := 1; i < 10000; i := i + i do {
		sys.Write( sys.Concat( sys.Concat("Hello World ",  sys.String(i) ), "\n") );
	}
		
	if i > 100 then {
		sys.Write( sys.Concat( sys.String(i), " is over 100\n" ) );
	} else {
		sys.Write( sys.Concat( sys.String(i), " is under 100\n" ) );
	}
	
	var lotsOfMemory := [10000] array(int);
	
	for i := 0; i < len(lotsOfMemory); i++ do {
		lotsOfMemory[i] := [1000]int;
	}
	
}