import("sorts/populate")
import("sorts/quick")
import("sys")

func main() {

	var list := [100]int;

	sys.Write("Before: ");	
	populate.PopulateWorstCase(list);
	populate.PrintList(list);
	
	quick.Sort(list);

	sys.Write("After: ");	
	populate.PrintList(list);
}