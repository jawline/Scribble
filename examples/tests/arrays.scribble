import ("sys")

func Test() : bool {
	var j : array(int)
	var r : array(string)
	var k : array(bool)
	
	r := [5]string
	r[0] := "Hello"
	r[1] := "World"
	r[2] := "How"
	r[3] := "Are"
	r[4] := "You"
	
	sys.String(len(r))
	
	for var i := 0; i < len(r); i++ {
		sys.Write(r[i] + "\n")
	}
	
	var jSize := 6
	j := [jSize]int

	if (len(j) != jSize) {
		return false
	}
	
	var arrayArray := [5]array(array(array(string)))
	
	return true
}