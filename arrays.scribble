import ("sys")

func Test() : bool {
	var j : array(int)
	var r : array(string)
	var k : array(bool)
	
	r := [5]string
	r[0] := "Hello"
	r[1] := "World"
	r[2] := "How"
	r[3] := "Are"
	r[4] := "You"
	
	var rLen := len(r)
	sys.Write("Length of r:"+sys.String(rLen)+"\n")
	
	for var i := 0; i < 5; i++ {
		sys.Write(r[i] + "\n")
	}
	
	var jSize := 6
	j := [jSize]int
	
	var arrayArray := [5]array(array(array(string)))
	
	return true
}