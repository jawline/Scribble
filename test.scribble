package math := import("examples/math/math");
package sys := import("sys");

func main() {

	for var i := 0; i < 100; i++ do {
		var j := math.Exp( sys.Float32(i) );
		sys.Write( sys.String ( j ) );
	}

}
